import { Asistencias5000Directive } from './asistencias5000.directive';

describe('Asistencias5000Directive', () => {
  it('should create an instance', () => {
    const directive = new Asistencias5000Directive();
    expect(directive).toBeTruthy();
  });
});
